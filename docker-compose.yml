services:
  api:
    build: .
    ports:
      - "8001:8000"
    depends_on:
      - db

  db:
    image:  mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: password  
      MYSQL_DATABASE: app_db
    ports:
      - "3307:3306"